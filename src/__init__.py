"""Shared package for the income bracket decision support application.

Modules here are imported by the notebook, the training scripts and the
Streamlit interface, so that all three apply identical logic. Run them as
modules from the repository root, for example ``python -m src.train``, never as
file paths.
"""